# Development defaults (auto-loaded by Vite in dev mode).
VITE_APP_ENV=development
VITE_API_URL=http://localhost:8080
# Alternative: use Vite proxy (no CORS) -> VITE_API_URL=/api then frontend hits backend via dev proxy
# VITE_API_URL=http://localhost:8080
